// var cache = {};$(function () {    // $("#field-message").delay(4000).fadeOut(600, function () {    //     $(this).remove();    // });    $(".btn-participate").click(function (e) {        var $self = $(this);        // $self.fadeOut(200, function () {        $self.addClass("hide");        $("#btn-remove-" + $self.data("value").replace("participate-", "")).removeClass("hide");        $.notify({            icon: 'glyphicon glyphicon-ok',            message: "Vous venez de participer à la séance: " + $self.data("title"),        }, {            type: "success",        });        // });        e.preventDefault();    });    $(".btn-reset").click(function (e) {        var $self = $(this);        // $self.fadeOut(200, function () {        $self.addClass("hide");        $("#btn-remove-" + $self.data("value").replace("participants-", "") + "-" + $self.data("participant-id")).removeClass("hide");        $.notify({            icon: 'glyphicon glyphicon-ok',            message: "Vous venez d'ajouter " + $self.data("participant") + " à la seance (" + $self.data("title") + ")",        }, {            type: "success",        });        // });        e.preventDefault();    });    /* Deleting User */    $(".btn-remove").click(function (e) {        var action = $(this).data("action");        $(".btn-ok-delete").data("action", action);        var idObject = $.trim($(this).data("value").replace(action + "-", ""))        $(".btn-ok-delete").data("value", idObject);        $(".btn-ok-delete").data("line", $(this).data("line"));        if (action == "seance") {            $(".modal-body").html("<h5>Vous voulez vraiment supprimer la séance (<b>" + $(this).data("title") + "</b>)  ?</h5>");            $(".modal-body h5").css("font-size", "16px");        } else if (action == "address") {            $(".modal-body").html("<h5>Vous voulez vraiment supprimer cette adresse (<b>" + $(this).data("title") + "</b>) ?</h5>");            $(".modal-body h5").css("font-size", "16px");        } else if (action == "participate") {            console.log($(this).attr("id"));            $(".btn-ok-delete").data("btn", $(this).attr("id"));            $(".modal-body").html("<h4>Vous voulez vraiment annuler votre participation à cette séance? </h4><h5 class='text-muted bold'>(" + $(this).data("title") + ")</h5>");            $(".modal-body h5").css("font-size", "16px");        } else if (action == "participants") {            console.log($(this).attr("id"));            $(".btn-ok-delete").data("btn", $(this).attr("id"));            $(".btn-ok-delete").data("participant-id", $(this).data("participant-id"));            $(".modal-body").html("<h4>Vous voulez vraiment retirer (<b>" + $(this).data("participant") + "</b>) de cette séance? </h4>");            $(".modal-body h5").css("font-size", "16px");        }        $('#remove-modal').modal();        e.preventDefault();    });    $(".btn-ok-delete").click(function (e) {        var $self = $(this);        var href = null;        var action = $self.data("action");        console.log(action);        var idObject = $.trim($self.data("value"));        var data = {id: idObject};        var removeObject = $.trim($self.data("line"));        if (action == "seance") {            href = Routing.generate('seances_remove');            //var username = $.trim($(this).data("username"));        } else if (action == "address") {            href = Routing.generate('user_address_remove');        } else if (action == "participate") {            href = Routing.generate('user_remove_participation');        } else if (action == "participants") {            console.log($self.data('btn'));            data.participant = $self.data("participant-id");            href = Routing.generate('user_remove_participant');        }        console.log(href);        console.log(data);        if (href != null && data != null) {            $.getJSON(href, data)                .done(function (data) {                    // console.log(data.result);                    if (data.result == "success") {                        if (action == "participate") {                            $("#" + $self.data('btn')).addClass("hide");                            $("#btn-participate-" + idObject).removeClass("hide");                            console.log("Participer");                        } else if (action == "participants") {                            $("#" + $self.data('btn')).addClass("hide");                            $("#btn-reset-" + idObject + "-" + $self.data("participant-id")).removeClass("hide");                        } else {                            $('#' + removeObject).fadeOut(500, function () {                                $(this).remove();                            });                        }                        $.notify({                            // options                            icon: 'glyphicon glyphicon-ok',                            message: data.message,                        }, {                            type: "success",                        });                    } else {                        $.notify({                            // options                            icon: 'glyphicon glyphicon-warning-sign',                            title: 'Error: ',                            message: 'Une erreur s\'est produit lors de la suppression de l\'utilisateur',                        }, {                            type: "danger",                        });                    }                });        }        e.preventDefault();    });    //$("#search_film").change(function () {    //    $("#icon_search_film").removeClass("fa-spin").removeClass("fa-spinner").addClass("fa-film");    //});});// Doc Ready