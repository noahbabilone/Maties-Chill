{% extends "MCBundle::main.html.twig" %}

{% block title %}
    Films - {{ parent() }}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {% stylesheets

    'bundles/mc/themes/global/plugins/cubeportfolio/css/cubeportfolio.css'
    'bundles/mc/themes/pages/css/portfolio.min.css'
    'bundles/mc/css/card.css'
    filter='cssrewrite' %}
    {#
   
    
    '#}
    <link rel="stylesheet" href="{{ asset_url }}"/>
    {% endstylesheets %}

{% endblock %}

{% block body_mc %}
    <div class="page-container">
        <div class="page-content">
            <div class="page-bar">
                <div class="row">
                    <div class="col-sm-12">
                        <ul class="page-breadcrumb">
                            <li>
                                <a href="{{ path('home_page') }}">Accueil</a>
                                <i class="fa fa-circle"></i>
                            </li>
                            <li>
                                <a href="{{ path('film_home') }}">Films</a>
                                <i class="fa fa-circle"></i>
                            </li>
                            {% if breadcrumb is defined %}
                                <li>
                                    <span>{{ breadcrumb }}</span>
                                </li>
                            {% endif %}
                        </ul>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-sm-2">
                    {% include("MCBundle:Includes:submenu.html.twig") %}
                </div>
                <div class="col-sm-10">
                    <div class="row">
                        <div class="col-sm-12">
                            <h5 class="page-title page-header"> Films</h5>
                        </div>

                        {#portlet light bordered#}
                        {% if lastFilms is defined %}
                            <div class="col-sm-12">
                                <h4 class="">
                                    <a href="{{ path("film_action", {"action":"last_add"}) }}">
                                        Récemment ajoutées</a>
                                </h4>
                            </div>


                            <div class="col-sm-12">
                                <div class="portfolio-content">
                                    <div id="js-grid-juicy-projects" class="cbp">
                                        {% for film in lastFilms %}
                                            <div class="cbp-item">
                                                <article class="card">
                                                    <header class="card__thumb">
                                                        <a href="{{ path("film_view", {"slug": film.id}) }}">
                                                            {% if film.poster is defined and film.poster %}

                                                                <img src="{{ film.poster }}"
                                                                     class=" pull-left margin-right"
                                                                     alt="{{ film.title }}">
                                                            {% else %}
                                                                <img src="../../uploads/images/400x300.png" width="240"
                                                                     height="235"
                                                                     class=" pull-left margin-right"
                                                                     alt="{{ film.title }}">
                                                            {% endif %}</a>
                                                    </header>
                                                    <div class="card__body">
                                                        {#<div class="card__category"><a#}
                                                                    {#href="{{ path("film_view", {"slug": film.id}) }}">Plus de détails</a></div>#}
                                                        <h5 class="card__title  font-yellow-casablanca">
                                                            {{ film.title |length > 26 ? film.title| capitalize|slice(0, 26) ~ '...' : film.title | capitalize }}

                                                        </h5>
                                                        <ul class="card__description list-unstyled">
                                                            <h5 class="sub_card_title">
                                                                <a href="{{ path("film_view", {"slug": film.id}) }}"
                                                                   class="font-yellow-casablanca">
                                                                    {{ film.title | capitalize }}
                                                                </a></h5>
                                                            {% if film.directors is defined and film.directors %}
                                                                <li><b>De: </b>{{ film.directors }}</li>
                                                            {% endif %}

                                                            {% if film.actors is defined and film.actors %}
                                                                <li><b>Avec: </b>{{ film.actors }}</li>
                                                            {% endif %}
                                                            {% if film.runtime is defined and film.runtime %}
                                                                <li><b>Durée: </b>
                                                                    {{ film.runtime | date("g") }}
                                                                    h{{ film.runtime | date("i") }}min
                                                                </li>
                                                            {% endif %}
                                                            {% if film.releaseDate is defined and film.releaseDate %}
                                                                <li><b>Année: </b>{{ film.releaseDate | date("Y") }}
                                                                </li>
                                                            {% endif %}
                                                            {% if film.genre is defined and film.genre %}
                                                                <li><b>Genre(s): </b>
                                                                    {% for genre in film.genre %}
                                                                        {{ genre.title }}
                                                                        {% if loop.index < film.genre |length %}/{% endif %}
                                                                    {% endfor %}
                                                                </li>
                                                            {% endif %}

                                                        </ul>
                                                    </div>
                                                    <footer class="card__footer text-right">
                                        <span class="">
                                            <a href="{{ path("film_view", {"slug": film.id}) }}"
                                               class="text-info">Suite&raquo;</a>
                                        </span>
                                                    </footer>
                                                </article>

                                            </div>

                                        {% endfor %}
                                    </div>
                                </div>
                            </div>
                            <div class="col-sm-12 text-right">
                                <a href="{{ path("film_action", {"action":"last_film"}) }}" class="">
                                    Tout voir &raquo;</a>
                            </div>
                        {% endif %}
                        {% if newFilms is defined %}
                            <div class="col-sm-12">

                                <h4 class="page-header">
                                    <a href="{{ path("film_action", {"action":"new_film"}) }}">
                                        Récemment ajoutées</a>
                                </h4>
                            </div>

                            <div class="col-sm-12">
                                <div class="portfolio-content portfolio-1">
                                    <div id="js-grid-juicy-projects" class="cbp">
                                        {% for film in newFilms %}
                                            <div class="cbp-item">
                                                <article class="card">
                                                    <header class="card__thumb">
                                                        <a href="{{ path("film_view", {"slug": film.id}) }}">
                                                            {% if film.poster is defined and film.poster %}

                                                                <img src="{{ film.poster }}"
                                                                     class=" pull-left margin-right"
                                                                     alt="{{ film.title }}">
                                                            {% else %}
                                                                <img src="../../uploads/images/400x300.png" width="240"
                                                                     height="235"
                                                                     class=" pull-left margin-right"
                                                                     alt="{{ film.title }}">
                                                            {% endif %}                                    </a>
                                                    </header>
                                                    <div class="card__body">
                                                        {#<div class="card__category"><a#}
                                                                    {#href="{{ path("film_view", {"slug": film.id}) }}">Plus de détails</a></div>#}
                                                        <h5 class="card__title  font-yellow-casablanca">
                                                            {{ film.title |length > 26 ? film.title| capitalize|slice(0, 26) ~ '...' : film.title | capitalize }}

                                                        </h5>
                                                        <ul class="card__description list-unstyled">
                                                            <h5 class="sub_card_title">
                                                                <a href="{{ path("film_view", {"slug": film.id}) }}"
                                                                   class="font-yellow-casablanca">
                                                                    {{ film.title | capitalize }}
                                                                </a></h5>

                                                            {% if film.directors is defined and film.directors %}
                                                                <li><b>De: </b>{{ film.directors }}</li>
                                                            {% endif %}

                                                            {% if film.actors is defined and film.actors %}
                                                                <li><b>Avec: </b>{{ film.actors }}</li>
                                                            {% endif %}
                                                            {% if film.runtime is defined and film.runtime %}
                                                                <li><b>Durée: </b>
                                                                    {{ film.runtime | date("g") }}
                                                                    h{{ film.runtime | date("i") }}min
                                                                </li>
                                                            {% endif %}
                                                            {% if film.releaseDate is defined and film.releaseDate %}
                                                                <li><b>Année: </b>{{ film.releaseDate | date("Y") }}
                                                                </li>
                                                            {% endif %}
                                                            {% if film.genre is defined and film.genre %}
                                                                <li><b>Genre(s): </b>
                                                                    {% for genre in film.genre %}
                                                                        {{ genre.title }}
                                                                        {% if loop.index < film.genre |length %}/{% endif %}
                                                                    {% endfor %}
                                                                </li>
                                                            {% endif %}

                                                        </ul>
                                                    </div>
                                                    <footer class="card__footer text-right">
                                        <span class="">
                                            <a href="{{ path("film_view", {"slug": film.id}) }}"
                                               class="text-info">Suite&raquo;</a>
                                        </span>
                                                    </footer>
                                                </article>

                                            </div>

                                        {% endfor %}

                                    </div>
                                </div>
                            </div>
                            <div class="col-sm-12 text-right">
                                <a href="{{ path("film_action", {"action":"new_film"}) }}" class="">
                                    Tout voir &raquo;</a>
                            </div>
                        {% endif %}
                        {% if topFilms is defined %}
                            <div class="col-sm-12">
                                <h4 class="page-header">
                                    <a href="{{ path("film_action", {"action":"top_film"}) }}">
                                        Récemment ajoutées</a>
                                </h4>
                            </div>

                            <div class="col-sm-12">
                                <div class="portfolio-content portfolio-1">
                                    <div id="js-grid-juicy-projects" class="cbp">
                                        {% for film in topFilms %}
                                            <div class="cbp-item">
                                                <article class="card">
                                                    <header class="card__thumb">
                                                        <a href="{{ path("film_view", {"slug": film.id}) }}">
                                                            {% if film.poster is defined and film.poster %}

                                                                <img src="{{ film.poster }}"
                                                                     class=" pull-left margin-right"
                                                                     alt="{{ film.title }}">
                                                            {% else %}
                                                                <img src="../../uploads/images/400x300.png" width="240"
                                                                     height="235"
                                                                     class=" pull-left margin-right"
                                                                     alt="{{ film.title }}">
                                                            {% endif %}                                    </a>
                                                    </header>
                                                    <div class="card__body">
                                                        {#<div class="card__category"><a#}
                                                                    {#href="{{ path("film_view", {"slug": film.id}) }}">Plus de détails</a></div>#}
                                                         <h5 class="card__title  font-yellow-casablanca">
                                                            {{ film.title |length > 26 ? film.title| capitalize|slice(0, 26) ~ '...' : film.title | capitalize }}

                                                        </h5>
                                                        <ul class="card__description list-unstyled">
                                                            <h5 class="sub_card_title">
                                                                <a href="{{ path("film_view", {"slug": film.id}) }}"
                                                                   class="font-yellow-casablanca">
                                                                    {{ film.title | capitalize }}
                                                                </a></h5>

                                                            {% if film.directors is defined and film.directors %}
                                                                <li><b>De: </b>{{ film.directors }}</li>
                                                            {% endif %}

                                                            {% if film.actors is defined and film.actors %}
                                                                <li><b>Avec: </b>{{ film.actors }}</li>
                                                            {% endif %}
                                                            {% if film.runtime is defined and film.runtime %}
                                                                <li><b>Durée: </b>
                                                                    {{ film.runtime | date("g") }}
                                                                    h{{ film.runtime | date("i") }}min
                                                                </li>
                                                            {% endif %}
                                                            {% if film.releaseDate is defined and film.releaseDate %}
                                                                <li><b>Année: </b>{{ film.releaseDate | date("Y") }}
                                                                </li>
                                                            {% endif %}
                                                            {% if film.genre is defined and film.genre %}
                                                                <li><b>Genre(s): </b>
                                                                    {% for genre in film.genre %}
                                                                        {{ genre.title }}
                                                                        {% if loop.index < film.genre |length %}/{% endif %}
                                                                    {% endfor %}
                                                                </li>
                                                            {% endif %}

                                                        </ul>
                                                    </div>
                                                    <footer class="card__footer text-right">
                                        <span class="">
                                            <a href="{{ path("film_view", {"slug": film.id}) }}"
                                               class="text-info">Suite&raquo;</a>
                                        </span>
                                                    </footer>
                                                </article>

                                            </div>

                                        {% endfor %}

                                    </div>
                                </div>
                            </div>
                            <div class="col-sm-12 text-right">
                                <a href="{{ path("film_action", {"action":"top_film"}) }}" class="">
                                    Tout voir &raquo;</a>
                            </div>
                        {% endif %}
                        {% if lastFilms is defined %}
                            <div class="col-sm-12">
                                <h5 class="page-header">
                                    <a href="{{ path("film_action", {"action":"last_add"}) }}">
                                        Récemment ajoutées</a>
                                </h5>
                            </div>

                            <div class="col-sm-12">
                                <div class="portfolio-content portfolio-1">
                                    <div id="js-grid-juicy-projects" class="cbp">
                                        {% for film in lastFilms %}
                                            <div class="cbp-item">
                                                <article class="card">
                                                    <header class="card__thumb">
                                                        <a href="{{ path("film_view", {"slug": film.id}) }}">
                                                            {% if film.poster is defined and film.poster %}

                                                                <img src="{{ film.poster }}"
                                                                     class=" pull-left margin-right"
                                                                     alt="{{ film.title }}">
                                                            {% else %}
                                                                <img src="../../uploads/images/400x300.png" width="240"
                                                                     height="235"
                                                                     class=" pull-left margin-right"
                                                                     alt="{{ film.title }}">
                                                            {% endif %} </a>
                                                    </header>
                                                    <div class="card__body">
                                                        {#<div class="card__category"><a#}
                                                                    {#href="{{ path("film_view", {"slug": film.id}) }}">Plus#}
                                                                {#de détails</a></div>#}
                                                        <h5 class="card__title  font-yellow-casablanca">
                                                            {{ film.title |length > 26 ? film.title| capitalize|slice(0, 26) ~ '...' : film.title | capitalize }}

                                                        </h5>
                                                        <ul class="card__description list-unstyled">
                                                        <h5 class="sub_card_title  font-yellow-casablanca">
                                                                <a href="{{ path("film_view", {"slug": film.id}) }}">
                                                                    {{ film.title | capitalize }}
                                                                </a></h5>

                                                            {% if film.directors is defined and film.directors %}
                                                                <li><b>De: </b>{{ film.directors }}</li>
                                                            {% endif %}

                                                            {% if film.actors is defined and film.actors %}
                                                                <li><b>Avec: </b>{{ film.actors }}</li>
                                                            {% endif %}
                                                            {% if film.runtime is defined and film.runtime %}
                                                                <li><b>Durée: </b>
                                                                    {{ film.runtime | date("g") }}
                                                                    h{{ film.runtime | date("i") }}min
                                                                </li>
                                                            {% endif %}
                                                            {% if film.releaseDate is defined and film.releaseDate %}
                                                                <li><b>Année: </b>{{ film.releaseDate | date("Y") }}
                                                                </li>
                                                            {% endif %}
                                                            {% if film.genre is defined and film.genre %}
                                                                <li><b>Genre(s): </b>
                                                                    {% for genre in film.genre %}
                                                                        {{ genre.title }}
                                                                        {% if loop.index < film.genre |length %}/{% endif %}
                                                                    {% endfor %}
                                                                </li>
                                                            {% endif %}

                                                        </ul>
                                                    </div>
                                                    <footer class="card__footer text-right">
                                        <span class="">
                                            <a href="{{ path("film_view", {"slug": film.id}) }}"
                                               class="text-info">Suite&raquo;</a>
                                        </span>
                                                    </footer>
                                                </article>

                                            </div>

                                        {% endfor %}

                                    </div>
                                </div>
                            </div>
                            <div class="col-sm-12 text-right">
                                <a href="{{ path("film_action", {"action":"new_film"}) }}" class="">Tout voir &raquo;</a>
                            </div>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {% javascripts
    'bundles/mc/themes/global/plugins/cubeportfolio/js/jquery.cubeportfolio.min.js'
    'bundles/mc/js/cubePortfolio-seance-home.js' %}
    {#  #}
    <script src="{{ asset_url }}"></script>
    {% endjavascripts %}
{% endblock %}
