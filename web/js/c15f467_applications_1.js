$(function () {    /* Deleting User */    $(".btn-delete-session").click(function (e) {        var session = $.trim($(this).attr("data-session").replace("session-", ""));        $("#id-seance").html(session);        $(".ok-delete-session").attr("data-session", session);        $(".ok-delete-session").attr("data-line", $(this).attr("data-session-line"));        $('#myModal').modal();        e.preventDefault();    });    $(".ok-delete-session").click(function (e) {        var href = Routing.generate('user_remove_session');        var idSession = $.trim($(this).attr("data-session"));        var line = $.trim($(this).attr("data-line"));        var data = {            id: idSession,        };        console.log(href + " " + idSession);        $.getJSON(href, data)            .done(function (data) {                //console.log(data);                $(".text-response-ajax").html(data.message);                if (data.result == "Success") {                    $('#' + line).fadeOut(500, function () {                        $(this).remove();                    });                    if ($(".response-ajax").hasClass("hide")) {                        if (!$(".response-ajax").hasClass("alert-success")) {                            $(".response-ajax").hasClass("alert-success");                        }                        if ($(".response-ajax").hasClass("alert-danger")) {                            $(".response-ajax").removeClass("alert-danger");                        }                        $(".response-ajax").fadeIn(300, function () {                            $(this).removeClass("hide");                        });                    }                } else {                    if ($(".response-ajax").hasClass("hide")) {                        if ($(".response-ajax").hasClass("alert-success")) {                            $(".response-ajax").removeClass("alert-success");                        }                        if (!$(".response-ajax").hasClass("alert-danger")) {                            $(".response-ajax").hasClass("alert-danger");                        }                        $(".response-ajax").fadeIn(300, function () {                            $(this).removeClass("hide");                        });                    }                }                $(".response-ajax").delay(35000).fadeOut(1000, function () {                    $(this).addClass("hide");                });            });        /*.fail(function (jqxhr, textStatus, error) {         var err = textStatus + ", " + error;         console.log("Request Failed: " + err);         })*/        ;        e.preventDefault();    });});// Doc Ready