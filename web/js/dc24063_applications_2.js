var cache = {};$(function () {    $("#field-message").delay(4000).fadeOut(600, function () {        $(this).remove();    });    console.log("Applications");    /* Deleting User */    $(".btn-remove").click(function (e) {        var action = $(this).attr("data-action");        $(".btn-ok-delete").attr("data-action", action);        var idObject = $.trim($(this).attr("data-value").replace(action + "-", ""))        $(".btn-ok-delete").attr("data-value", idObject);        $(".btn-ok-delete").attr("data-line", $(this).attr("data-line"));        if (action == "session") {            $(".modal-body").html("<h5>Vous voulez vraiment supprimer la séance (<b>"+ $(this).attr("data-title") + "</b>)  ?</h5>");            $(".modal-body h5").css("font-size", "16px");        } else if (action == "address") {            $(".modal-body").html("<h5>Vous voulez vraiment supprimer cette adresse (<b>" + $(this).attr("data-title") + "</b>) ?</h5>");            $(".modal-body h5").css("font-size", "16px");        }        $('#remove-modal').modal();        e.preventDefault();    });    $(".btn-ok-delete").click(function (e) {        var href = null;        var action = $(this).attr("data-action");        var idObject = $.trim($(this).attr("data-value"));        var data = {id: idObject};        var removeObject = $.trim($(this).attr("data-line"));        if (action == "session") {            href = Routing.generate('session_remove');            //var username = $.trim($(this).attr("data-username"));        } else if (action == "address") {            href = Routing.generate('user_address_remove');        }        if (href != null && data != null) {            $.getJSON(href, data)                .done(function (data) {                    console.log(data.result);                    if (data.result == "success") {                        $('#' + removeObject).fadeOut(500, function () {                            $(this).remove();                        });                        $.notify({                            // options                            icon: 'glyphicon glyphicon-ok',                            message: data.message,                        }, {                            type: "success",                        });                    } else {                        $.notify({                            // options                            icon: 'glyphicon glyphicon-warning-sign',                            title: 'Error: ',                            message: 'Une erreur s\'est produit lors de la suppression de l\'utilisateur',                        }, {                            type: "danger",                        });                    }                });        }        e.preventDefault();    });    //$("#search_film").change(function () {    //    $("#icon_search_film").removeClass("fa-spin").removeClass("fa-spinner").addClass("fa-film");    //});});// Doc Ready